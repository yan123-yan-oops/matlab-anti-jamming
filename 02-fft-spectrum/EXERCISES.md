# 02 习题：FFT 频谱分析
# 对应 C 02-pointers

---

### ✅ 概念题

1. FT 和 FFT 是什么关系？为什么 FFT 比 FT 快？
2. 频率分辨率由什么决定？怎么提高？
3. 什么是频谱泄露？加窗为什么能缓解？
4. 为什么 FFT 结果是对称的？
5. 采样定理是什么？采样率不够会怎样？（和 C 的数组越界类比）

---

### 🔍 代码阅读题

**题 A：**
```matlab
Fs = 200; t = 0:1/Fs:1-1/Fs;
x = sin(2*pi*5*t) + sin(2*pi*30*t);
X = fft(x);
mag = abs(X) / length(x) * 2;
% 频谱在哪些频率有峰值？
```

**题 B：**
```matlab
x = [1 2 3 4];
X = fft(x);
% X(1) 等于什么？代表什么？
% 提示：X(1) = sum(x)
```

---

### 💻 编程题

**题 1：生成一个含噪声的正弦波，用 FFT 找出信号频率。**
```matlab
% 生成: t, y = sin(2π·17·t) + 0.5·randn(size(t))
% FFT → 找到峰值频率 → 应该接近 17Hz
```

**题 2：对比加窗和不加窗的频谱效果。**
```matlab
% 生成一个非整数周期的正弦波
% 分别用矩形窗（不加窗）和汉宁窗做 FFT
% 画出两个频谱对比
```

**题 3：用 FFT 分析你录的一段声音或生成的音频。**
```matlab
% 生成一个 DTMF 双音多频信号（电话按键音）
% 697Hz + 1209Hz 对应按键 '1'
% 用 FFT 找出这两个频率
```
